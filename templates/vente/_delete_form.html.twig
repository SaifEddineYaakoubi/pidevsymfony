<form method="post" action="{{ path('app_vente_delete', {'id_vente': vente.id_vente}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vente.id_vente) }}">
    <button class="btn">Delete</button>
</form>
