{% extends 'front/base_agriculteur.html.twig' %}

{% block title %}Éditer Vente - AgriCulture{% endblock %}

{% block agriculteur_body %}
    <div class="container section">
        <div class="row">
            <div class="col-lg-8">
                <h1 class="mb-4">Éditer la Vente</h1>

                {{ include('vente/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                <a href="{{ path('app_vente_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>

                {{ include('vente/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
