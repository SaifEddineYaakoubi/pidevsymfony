{% extends 'front/base_agriculteur.html.twig' %}

{% block title %}Éditer Client - AgriCulture{% endblock %}

{% block agriculteur_body %}
    <div class="container section">
        <div class="row">
            <div class="col-lg-8">
                <h1 class="mb-4">Éditer le Client</h1>

                {{ include('client/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

                <a href="{{ path('app_client_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>

                {{ include('client/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
